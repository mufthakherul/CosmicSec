# CosmicSec Redis Configuration
# Password is injected via the REDIS_PASSWORD environment variable
# at container start using the docker-compose command override.

# Persistence
appendonly yes
appendfsync everysec

# Memory
maxmemory-policy allkeys-lru

# Network
bind 0.0.0.0
protected-mode no
